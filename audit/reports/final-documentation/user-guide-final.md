# دليل المستخدم النهائي - SynapsePay

## مقدمة

مرحباً بك في SynapsePay، منصة الدفع المصغر المدعومة بالذكاء الاصطناعي على شبكة Solana. هذا الدليل سيساعدك على فهم واستخدام جميع ميزات النظام.

## البدء السريع

### 1. إعداد البيئة
```bash
# تثبيت التبعيات
npm install

# إعداد Solana
solana config set --url devnet

# بناء المشروع
anchor build
```

### 2. نشر العقود
```bash
# نشر على devnet
anchor deploy

# التحقق من النشر
solana program show <PROGRAM_ID>
```

### 3. تشغيل الاختبارات
```bash
# اختبارات الوحدة
anchor test

# اختبارات التكامل
./audit/run-integration-performance-tests-final.sh

# اختبارات النظام الشاملة
./audit/comprehensive-system-test.sh
```

## الميزات الرئيسية

### 🤖 الوكلاء الذكيون
- تسجيل وكلاء ذكيين جدد
- إدارة الوكلاء الموجودين
- تنفيذ المهام والحصول على النتائج

### 💰 نظام المدفوعات
- إنشاء فواتير للخدمات
- معالجة المدفوعات بأمان
- إصدار إيصالات NFT
- تسوية المدفوعات تلقائياً

### 📅 المهام المجدولة
- إنشاء اشتراكات دورية
- إدارة المهام المجدولة
- مراقبة الرصيد والاستخدام

### 🌐 واجهة المستخدم
- لوحة تحكم شاملة
- عرض الإحصائيات والتقارير
- إدارة الحساب والإعدادات

## أمثلة الاستخدام

### مثال 1: تسجيل وكيل ذكي
```typescript
const agent = {
  name: "مساعد الكتابة",
  description: "وكيل ذكي لمساعدة في الكتابة",
  price: 1000000, // 1 USDC
  metadata: "QmHash..."
};

await registerAgent(agent);
```

### مثال 2: إنشاء فاتورة
```typescript
const invoice = {
  agentId: "agent-123",
  amount: 1000000,
  description: "خدمة كتابة مقال"
};

await createInvoice(invoice);
```

### مثال 3: إنشاء اشتراك
```typescript
const subscription = {
  agentId: "agent-123",
  balance: 10000000, // 10 USDC
  cadence: "daily",
  maxRuns: 30
};

await createSubscription(subscription);
```

## استكشاف الأخطاء

### مشاكل شائعة وحلولها

#### خطأ في الاتصال بـ Solana
```bash
# التحقق من الإعدادات
solana config get

# إعادة تعيين الشبكة
solana config set --url devnet
```

#### مشاكل في البناء
```bash
# تنظيف وإعادة البناء
anchor clean
anchor build
```

#### مشاكل في النشر
```bash
# التحقق من الرصيد
solana balance

# طلب SOL للاختبار
solana airdrop 2
```

## الأمان والأفضل الممارسات

### حماية المفاتيح
- احتفظ بالمفاتيح الخاصة في مكان آمن
- لا تشارك المفاتيح مع أحد
- استخدم محافظ أجهزة للمبالغ الكبيرة

### أفضل الممارسات
- اختبر دائماً على devnet أولاً
- راجع الكود قبل النشر
- احتفظ بنسخ احتياطية من المفاتيح
- راقب النشاط بانتظام

## الدعم والمساعدة

### الموارد المفيدة
- [وثائق Solana](https://docs.solana.com)
- [وثائق Anchor](https://anchor-lang.com)
- [مجتمع SynapsePay](https://github.com/synapsepay)

### الحصول على المساعدة
- افتح issue في GitHub
- انضم لمجتمع Discord
- راسلنا على البريد الإلكتروني

---
*آخر تحديث: $(date)*
