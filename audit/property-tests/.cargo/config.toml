# Override workspace config for property tests
# These tests run on the host machine, not on Solana BPF

[build]
# Use default target (host machine) instead of Solana BPF
target-dir = "target"

[env]
# Ensure we're not using Solana-specific environment variables
CARGO_CFG_TARGET_ARCH = ""